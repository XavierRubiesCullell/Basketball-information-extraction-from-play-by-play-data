def read_plays(f):
    end = False
    while not end:
        line = f.readline().strip()
        if line != '':
            treat_line(line)
        else:
            end = True

# f = open('./Prova.txt', 'r')
# print(f.read())
    # for line in f:
    #     # reading each word         
    #     for word in line.split(): 
    #         # displaying the action            
    #         print(word)